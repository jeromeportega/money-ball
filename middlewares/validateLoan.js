module.exports = (req, res, next) => {
    // TODO: Validate Request Loan Object Fields Here to make sure they are all present
    // Make sure to include this middleware in the addloan route

    next();
};
